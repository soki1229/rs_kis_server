fn main() {
    eprintln!("rs_kis_server is a framework library.");
    eprintln!("Create a binary crate that implements StrategyBundle and calls kis_server::run().");
    eprintln!("Example: rs_kis_pilot");
    std::process::exit(1);
}
